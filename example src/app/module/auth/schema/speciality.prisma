model Specialty {
  id                String            @id @default(uuid(7))
  icon              String?           @db.Text
  title             String            @unique @db.VarChar(100)
  description       String?           @db.Text
  isDelete          Boolean           @default(false)
  deletedAt         DateTime?
  createdAt         DateTime          @default(now())
  updatedAt         DateTime          @updatedAt
  doctorSpecialties DoctorSpecialty[]

  @@index([isDelete], name: "idx_specialty_isDeleted")
  @@index([title], name: "idx_specialty_title")
  @@map("specialties")
}

model DoctorSpecialty {

  doctorId    String
  specialtyId String

  doctor      Doctor    @relation(fields: [doctorId], references: [id],onDelete: Cascade, onUpdate: Cascade)
  specialties Specialty @relation(fields: [specialtyId], references: [id], onDelete: Cascade, onUpdate: Cascade)

  @@id([doctorId, specialtyId]) 
  @@index([doctorId,specialtyId], name:"idx_doctor_specialty_doctorId")
  @@index([specialtyId])

  @@map("doctor_specialties")
}
